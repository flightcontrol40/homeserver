apiVersion: v1
kind: Service
metadata:
  name: {{ include "minecraftServer.fullname" . }}
  labels:
    {{- include "minecraftServer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "minecraftServer.selectorLabels" . | nindent 4 }}
